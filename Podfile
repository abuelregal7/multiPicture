project 'multiPicture.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'multiPicture' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod "BSImagePicker", "~> 2.8"
  # Pods for multiPicture

  target 'multiPictureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'multiPictureUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
